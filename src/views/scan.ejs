<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhatsApp Gateway - Scan QR</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-900 font-sans flex items-center justify-center h-screen">
    <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md text-center">
        <h1 class="text-2xl font-semibold mb-4">WhatsApp Gateway</h1>
        <% if (qrimage) { %>
            <img src="<%= qrimage %>" alt="QR Code" class="w-48 h-48 mx-auto mb-6 border-2 border-gray-300 rounded-md"/>
            <p class="text-gray-700">Scan this QR Code to connect your WhatsApp.</p>
        <% } else { %>
            <p class="text-green-600">Client is ready!</p>
        <% } %>
    </div>
</body>
</html>
